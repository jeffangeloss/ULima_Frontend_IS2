/// Client ID web de Google OAuth.
///
/// Es **público** (va embebido en la página web), así que es seguro versionarlo.
/// El `client_secret` NO se usa en el frontend (solo en flujos de servidor).
const String googleWebClientId =
    '608661962945-ja2030ua9i1tot9ihnhaeirt8b6fuli8.apps.googleusercontent.com';
